"""DevTeam engine — multi-session agent orchestration (FEAT-420).

Clean Architecture: domain -> application -> infrastructure -> interface.
Filesystem-only, stdlib-only core. JSON on stdout.
"""

__version__ = "0.1.0"
