{
  "$contract": "vir/runtime_request/v1",
  "title": "Runtime Request Contract Specification",
  "version": "1.0.0",
  "envelope": {
    "session_id": {
      "type": "string",
      "description": "Unique session index."
    },
    "action": {
      "type": "string",
      "description": "Deterministic request action verb."
    },
    "payload": {
      "type": "object",
      "description": "Metadata arguments coordinates."
    }
  },
  "required": [
    "session_id",
    "action"
  ]
}
