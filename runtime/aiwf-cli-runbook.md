# AIWF CLI Runbook
This document is auto-generated by `runbook_generator.py`. **DO NOT EDIT MANUALLY.**
It provides a complete reference for all commands, flags, and options available in the AIWF CLI.

## 🤖 AI Agent Instructions (Mandatory)
When you are instructed to use the `aiwf` CLI, you **MUST** follow these rules:
1. **Never guess commands**: If a command or flag is not explicitly listed in this document, it does not exist. Do not hallucinate or invent commands.
2. **Strict syntax**: Pay attention to required arguments vs optional flags.
3. **No extra quotes**: Do not add unnecessary quotes around arguments unless the shell requires it to escape spaces.
4. **Native Execution**: You are operating in a shell environment where `aiwf` is a globally available command. Do not use `python -m workflow_runtime` unless explicitly asked to bypass the wrapper.

## Release Publication Checkpoint (Mandatory)
For this repository, a local version bump plus `make export` only means the release is **prepared locally**. GitHub does not receive a new version until the nested `public_export` repository is committed, tagged, and pushed.

When publishing a release that includes `public_export`, agents MUST use this order:
1. Confirm explicit release approval for git write actions (`Continue|Cancel`).
2. Commit and tag the nested `public_export` repository first.
3. Push `public_export` `main` and the release tag to GitHub.
4. Return to the parent repository, stage the updated `public_export` submodule pointer plus release files.
5. Commit, tag, and push the parent repository.
6. Install the published framework back into the current project so local `.agents/` matches the released package:
   - Windows PowerShell: `aiwf update -Force`
   - Unix shells: `aiwf update --force`
7. Verify the local project sees the released version with `aiwf status` or the project version command.

Verification commands after publication:
```powershell
git -C public_export ls-remote --heads origin main
git -C public_export ls-remote --tags origin vX.Y.Z
git ls-remote --heads origin main
git ls-remote --tags origin vX.Y.Z
aiwf update -Force
aiwf status
git status --short --untracked-files=all
git -C public_export status --short --untracked-files=all
```

If the target tag is missing from `git -C public_export ls-remote --tags origin vX.Y.Z`, GitHub has not received the public release yet. Do not report the release as published.

---

## `aiwf`

**Subcommands:**
### `aiwf start`

**Arguments / Flags:**
- `--skill` (Required): Skill name to start
- `--command` (Required): Command being executed
- `--checkpoint` (Optional):
- `--step` (Optional):

### `aiwf step`

**Arguments / Flags:**
- `--step` (Required): Step description
- `--log` (Optional): Log message

### `aiwf complete`

**Arguments / Flags:**
- `--checkpoint` (Optional):
- `--step` (Optional):
- `--next-skill` (Optional):
- `--next-command` (Optional):

### `aiwf fail`

**Arguments / Flags:**
- `--step` (Optional):
- `--log` (Optional):

### `aiwf heartbeat`

### `aiwf status`

### `aiwf resume`

### `aiwf lock`

**Arguments / Flags:**
- `action` (Required):  [Choices: acquire, release, list, inspect, recover, status, force-release]
- `--task-id` (Optional):
- `--files` (Optional):
- `--stale-only` (Optional):

### `aiwf init`

**Arguments / Flags:**
- `path` (Optional): Target directory for initialization
- `--force` (Optional): Force re-initialise existing workspace
- `--skill` (Optional): Install specific skill only
- `--template` (Optional): Workspace template to use
- `--no-git` (Optional): Skip git repository setup
- `--quiet` (Optional): Minimal output
- `--non-interactive` (Optional): Run in non-interactive mode
- `--config` (Optional): Path to config file for non-interactive mode
- `--resume` (Optional): Resume from previous interactive session draft

### `aiwf validate`

**Arguments / Flags:**
- `--checkpoint` (Optional): Checkpoint number to validate
- `--spec` (Optional): Spec file path
- `--strict` (Optional):

**Subcommands:**
#### `aiwf validate blueprint`

**Arguments / Flags:**
- `--file` (Required): Path to blueprint file

#### `aiwf validate artifact`

**Arguments / Flags:**
- `--file` (Required): Path to artifact file

#### `aiwf validate session`

### `aiwf context`

**Arguments / Flags:**
- `--refresh` (Optional):
- `--format` (Optional):  [Choices: json, text]

### `aiwf state`

**Subcommands:**
#### `aiwf state status`

#### `aiwf state recover`

#### `aiwf state validate`

#### `aiwf state doctor`

#### `aiwf state snapshot`

#### `aiwf state migrate`

#### `aiwf state aggregate`

#### `aiwf state emit`

#### `aiwf state diagnose`

### `aiwf usage`

**Arguments / Flags:**
- `--format` (Optional):  [Choices: json, table, text]
- `--history` (Optional): Show usage history
- `--provider` (Optional): Filter by provider
- `--limit` (Optional):

### `aiwf session`

**Subcommands:**
#### `aiwf session show`

#### `aiwf session list`

#### `aiwf session clean`

#### `aiwf session delete`

**Arguments / Flags:**
- `--id` (Required): Session ID

### `aiwf workflow`

**Subcommands:**
#### `aiwf workflow status`

#### `aiwf workflow history`

#### `aiwf workflow create`

**Arguments / Flags:**
- `--skill` (Required):

#### `aiwf workflow submit`

**Arguments / Flags:**
- `--prompt` (Required):

### `aiwf active-workflow`

**Arguments / Flags:**
- `--json` (Optional):

### `aiwf coordinator`

### `aiwf dispatch`

**Arguments / Flags:**
- `--agent` (Required): Agent role to dispatch
- `--task` (Optional): Task description
- `--skill` (Optional): Target skill

### `aiwf routing`

**Arguments / Flags:**
- `--intent` (Optional): Raw intent to classify

### `aiwf discover`

### `aiwf classify`

**Arguments / Flags:**
- `--request` (Required): User request to classify

### `aiwf orchestrator`

**Arguments / Flags:**
- `action` (Optional):  [Choices: run, status, cancel, resume]
- `--work-item` (Optional): Work item ID

### `aiwf orchestrate`

**Arguments / Flags:**
- `action` (Optional):  [Choices: run, status, cancel, resume]
- `--work-item` (Optional): Work item ID

### `aiwf task`

**Subcommands:**
#### `aiwf task list`

#### `aiwf task status`

#### `aiwf task create`

**Arguments / Flags:**
- `--name` (Required):
- `--agent` (Required):

#### `aiwf task update`

**Arguments / Flags:**
- `--id` (Required):
- `--status` (Optional):

### `aiwf blueprint`

**Arguments / Flags:**
- `action` (Optional):  [Choices: generate, validate, freeze, status]
- `--work-item` (Optional): Work item ID
- `--skill` (Optional): Target skill

### `aiwf suggest`

**Arguments / Flags:**
- `--limit` (Optional):

### `aiwf compact`

**Arguments / Flags:**
- `--target-tokens` (Optional):

### `aiwf work-item`

**Arguments / Flags:**
- `--id` (Optional): Work item ID
- `--format` (Optional):  [Choices: json, text]

**Subcommands:**
#### `aiwf work-item detect`

### `aiwf project`

**Arguments / Flags:**
- `--format` (Optional):  [Choices: json, text]

**Subcommands:**
#### `aiwf project version`

### `aiwf implement`

**Arguments / Flags:**
- `--blueprint` (Required): Blueprint file path
- `--dry-run` (Optional):

### `aiwf deps`

**Arguments / Flags:**
- `--skill` (Optional): Target skill for deps check
- `--force` (Optional):
- `--format` (Optional):  [Choices: json, text, table]

**Subcommands:**
#### `aiwf deps resolve`

#### `aiwf deps check`

#### `aiwf deps install`

#### `aiwf deps list`

#### `aiwf deps status`

#### `aiwf deps inspect`

#### `aiwf deps validate`

### `aiwf dependency`

**Arguments / Flags:**
- `action` (Optional):  [Choices: graph, validate, scan, report]
- `--output` (Optional): Output file path

### `aiwf merge`

**Arguments / Flags:**
- `--from` (Optional): Source agent ID
- `--strategy` (Optional):  [Choices: latest, oldest, manual]

### `aiwf conflict`

**Arguments / Flags:**
- `action` (Optional):  [Choices: detect, resolve, list]
- `--auto` (Optional): Auto-resolve conflicts

### `aiwf config`

**Arguments / Flags:**
- `--check-only` (Optional): Only report configuration status
- `--no-start` (Optional): Do not start runtime services

**Subcommands:**
#### `aiwf config show`

#### `aiwf config validate`

#### `aiwf config reset`

#### `aiwf config get`

**Arguments / Flags:**
- `key` (Required):

#### `aiwf config set`

**Arguments / Flags:**
- `key` (Required):
- `value` (Required):

### `aiwf permission`

**Subcommands:**
#### `aiwf permission list`

#### `aiwf permission status`

#### `aiwf permission grant`

**Arguments / Flags:**
- `--agent` (Optional):
- `--action` (Optional):

#### `aiwf permission revoke`

**Arguments / Flags:**
- `--agent` (Optional):
- `--action` (Optional):

### `aiwf permissions`

**Subcommands:**
#### `aiwf permissions list`

#### `aiwf permissions status`

#### `aiwf permissions grant`

**Arguments / Flags:**
- `--agent` (Optional):
- `--action` (Optional):

#### `aiwf permissions revoke`

**Arguments / Flags:**
- `--agent` (Optional):
- `--action` (Optional):

### `aiwf rules`

**Subcommands:**
#### `aiwf rules status`

### `aiwf registry`

**Arguments / Flags:**
- `--format` (Optional):  [Choices: json, table, text]

**Subcommands:**
#### `aiwf registry register`

**Arguments / Flags:**
- `path` (Optional): Project path (default: current dir)
- `--force` (Optional): Re-register even if already registered
- `--source` (Optional): Registration source tag
- `--framework-root` (Optional):

#### `aiwf registry unregister`

**Arguments / Flags:**
- `path` (Optional): Project path to remove

#### `aiwf registry list`

#### `aiwf registry doctor`

#### `aiwf registry cleanup`

### `aiwf execution`

**Subcommands:**
#### `aiwf execution list`

#### `aiwf execution status`

#### `aiwf execution cancel`

#### `aiwf execution log`

### `aiwf runtime`

**Subcommands:**
#### `aiwf runtime start`

#### `aiwf runtime stop`

#### `aiwf runtime status`

#### `aiwf runtime restart`

#### `aiwf runtime reload`

#### `aiwf runtime enable`

#### `aiwf runtime disable`

#### `aiwf runtime process`

#### `aiwf runtime daemon`

#### `aiwf runtime policy`

**Arguments / Flags:**
- `policy_action` (Optional): Policy action [Choices: validate, reset]

### `aiwf analysis-agent`

**Arguments / Flags:**
- `action` (Optional):  [Choices: code, architecture, performance, security, accessibility]
- `--target` (Optional): Target file or directory
- `--output` (Optional): Output report path
- `--format` (Optional):  [Choices: json, markdown, text]

### `aiwf prompt`

**Subcommands:**
#### `aiwf prompt select`

**Arguments / Flags:**
- `--question` (Required):
- `--options` (Required): Pipe-separated options e.g. 'Continue|Cancel'
- `--default` (Optional):

#### `aiwf prompt confirm`

**Arguments / Flags:**
- `--question` (Required):
- `--default` (Optional):  [Choices: yes, no]

### `aiwf input`

**Subcommands:**
#### `aiwf input submit`

**Arguments / Flags:**
- `--input-id` (Required):
- `--value` (Required):
- `--source` (Required):
- `--resume-token` (Required):

### `aiwf choice`

**Arguments / Flags:**
- `--title` (Optional): Menu title
- `--options` (Required): Newline or pipe-separated choices
- `--default` (Optional):

### `aiwf memory`

**Arguments / Flags:**
- `subaction` (Optional): Memory action to perform [Choices: bootstrap, update, query, status, reset, export]
- `--query` (Optional): Search query for memory lookup
- `--limit` (Optional): Max results to return
- `--format` (Optional):  [Choices: json, text, table]

### `aiwf env`

**Arguments / Flags:**
- `--filter` (Optional): Filter by key prefix
- `--json` (Optional): Output as JSON

### `aiwf mail`

**Arguments / Flags:**
- `subaction` (Required):  [Choices: register, send, read, list]
- `--to` (Optional):
- `--message` (Optional):

### `aiwf knowledge`

**Arguments / Flags:**
- `action` (Optional): Knowledge action [Choices: index, query, status, rebuild, clear, export]
- `--query` (Optional): Semantic search query
- `--limit` (Optional): Max results (default: 5)
- `--provider` (Optional): Vector store provider [Choices: qdrant, sqlite, memory]
- `--collection` (Optional): Collection/index name
- `--format` (Optional):  [Choices: json, text]

### `aiwf search`

**Arguments / Flags:**
- `query` (Optional): Search query string
- `--query` (Optional): Search query (flag form)
- `--limit` (Optional):
- `--provider` (Optional): Vector store provider
- `--format` (Optional):  [Choices: json, text]

### `aiwf visual`

**Subcommands:**
#### `aiwf visual agent`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf visual investigate`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf visual verify`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf visual memory`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf visual report`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf visual observe`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

### `aiwf vir`

**Subcommands:**
#### `aiwf vir agent`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf vir investigate`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf vir verify`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf vir memory`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf vir report`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf vir observe`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

### `aiwf var`

**Subcommands:**
#### `aiwf var agent`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf var investigate`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf var verify`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf var memory`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf var report`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

#### `aiwf var observe`

**Arguments / Flags:**
- `--url` (Optional):
- `--goal` (Optional):
- `--max-iter` (Optional):
- `--mode` (Optional):  [Choices: cli, ipc, daemon]
- `--feature-id` (Optional):
- `--ci` (Optional):

### `aiwf telegram`

**Subcommands:**
#### `aiwf telegram send`

**Arguments / Flags:**
- `--message`, `-m` (Required): Message text
- `--chat-id` (Optional): Target chat ID (overrides config)
- `--token` (Optional): Bot token (overrides config)
- `--parse-mode` (Optional):  [Choices: HTML, Markdown]

#### `aiwf telegram notify`

**Arguments / Flags:**
- `--message`, `-m` (Required):
- `--level` (Optional):  [Choices: info, warning, error, success]

#### `aiwf telegram start`

#### `aiwf telegram stop`

#### `aiwf telegram status`

#### `aiwf telegram restart`

#### `aiwf telegram link`

#### `aiwf telegram config`

#### `aiwf telegram test`

### `aiwf provider`

**Arguments / Flags:**
- `action` (Optional): Provider action [Choices: list, select, config, test, usage, status, reset, add, remove]
- `--name` (Optional): Provider name
- `--model` (Optional): Model name
- `--api-key` (Optional): API key (stored securely)
- `--base-url` (Optional): Custom base URL
- `--timeout` (Optional):
- `--format` (Optional):  [Choices: json, table, text]

### `aiwf cleanup`

**Arguments / Flags:**
- `--dry-run` (Optional): Preview changes without writing
- `--target` (Optional): Target directory to clean
- `--backup` (Optional): Create backup before cleanup

### `aiwf migrate`

**Subcommands:**
#### `aiwf migrate state`

### `aiwf migration`

**Subcommands:**
#### `aiwf migration state`

### `aiwf api-server`

**Arguments / Flags:**
- `--port` (Optional):
- `--host` (Optional):
- `--debug` (Optional):

### `aiwf doctor`

**Arguments / Flags:**
- `--fix` (Optional): Auto-fix issues
- `--verbose` (Optional):
- `--check` (Optional): Run specific check only

### `aiwf notify`

**Arguments / Flags:**
- `--message`, `-m` (Required):
- `--level` (Optional):  [Choices: info, warning, error, success]

### `aiwf debug`

**Arguments / Flags:**
- `action` (Optional):  [Choices: logs, state, session, memory, env]
- `--tail` (Optional):

### `aiwf verify`

**Arguments / Flags:**
- `--blueprint` (Optional): Blueprint file path
- `--strict` (Optional):

### `aiwf release`

**Arguments / Flags:**
- `action` (Optional):  [Choices: validate, tag, publish, rollback, status, plan, execute]
- `--version` (Optional):
- `--dry-run` (Optional):
- `--approve` (Optional):

### `aiwf post-release`

**Arguments / Flags:**
- `action` (Optional):  [Choices: run, status]
- `--version` (Optional):
- `--commit` (Optional):
- `--output-dir` (Optional):

### `aiwf update`

**Arguments / Flags:**
- `action` (Optional):  [Choices: framework, skills, runtime, all]
- `--force` (Optional):
- `--dry-run` (Optional):
- `--version` (Optional): Target version

### `aiwf update-source`

**Arguments / Flags:**
- `--branch` (Optional): Target branch
- `--tag` (Optional): Target tag
- `--dry-run` (Optional):
- `--no-install` (Optional): Skip pip install after update
- `--allow-dirty` (Optional): Ignore local uncommitted changes when updating

### `aiwf test`

**Arguments / Flags:**
- `scope` (Optional): Test scope (default: smoke) [Choices: unit, smoke, integration, all, real]
- `--verbose`, `-v` (Optional):
- `--fail-fast` (Optional): Stop on first failure
- `--output` (Optional): Output report path
- `--filter`, `-k` (Optional): Filter tests by name pattern
- `--log` (Optional): Log file path (default: .agents/runtime/tests.log)
